# 07-tests — Planos e Protocolos de Teste

Esta pasta é destinada a planos e protocolos de teste formais do DocsViewer Omni (testes de aceitação, roteiros de validação funcional, etc.), distintos dos testes automatizados de código (`DocsViewer.UnitTests`, `DocsViewer.IntegrationTests`).

## Status

**Sem planos de teste formais ainda.** Nenhum ID de plano de teste foi inventado.

Testes automatizados de código já existem como projetos na solution (`DocsViewer.UnitTests`, `DocsViewer.IntegrationTests`); seus resultados são registrados em `DEV_STATUS.md` a cada tarefa. Protocolos de teste formais (para fins de validação/homologação) dependem dos requisitos (URS) e regras de negócio (BRN) ainda ausentes — ver `docs/handoff/OPEN_QUESTIONS.md` (Q-004).
